<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Example App</title>
	
	<style>
		header {
			width: 100%;
			height: 150px;
			background-color: #3250bd;
			text-align: center;
		}
		header h1 {
			font-family: Tahoma, Geneva, sans-serif;
			font-size: xx-large;
			color: #efe3e;
		}
		header img {
			width: 400px;
		}
		.login-form {
			width: 300px;
			margin: 0 auto;
			font-family: Tahoma, Geneva, sans-serif;
		}
		.login-form h1 {
			text-align: center;
			color: #4d4d4d;
			font-size: 24px;
			padding: 20px 0 20px 0;
		}
		.login-form input[type="password"],
		.login-form input[type="text"] {
			width: 100%;
			padding: 15px;
			border: 1px solid #dddddd;
			margin-bottom: 15px;
			box-sizing:border-box;
		}
		.login-form input[type="submit"],
		.login-form input[type="button"] {
			width: 100%;
			padding: 15px;
			background-color: #535b63;
			border: 0;
			box-sizing: border-box;
			cursor: pointer;
			font-weight: bold;
			color: #ffffff;
		}
		footer {
			width: 100%;
			height: 100px;
			position: fixed;
			margin-bottom: 0;
			text-align: center;
		}
		
		footer p {
			font-family: Tahoma, Geneva, sans-serif;
			font-size: small;
		}
		</style>
</head>
<body>
	<header>
		<h1>Auckland Property Management</h1>
		<img src="houses-logo.png" alt="APM Logo"/>
	</header>
	{{#if showTitle}}
		<h1>Home</h1>
	{{/if}}

		<!-- Calls `foo` helper, overridden at render-level. -->
		<p>{{foo}}</p>

		<!-- Calls `bar` helper, defined at instance-level. -->
		<p>{{bar}}</p>

    {{{body}}}

	<footer>
		<p>Copyright &copy; Auckland Property Management 2019</p>
	</footer>
</body>
</html>